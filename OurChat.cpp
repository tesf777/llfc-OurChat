#include "OurChat.h"

OurChat::OurChat(QWidget *parent)
    : QMainWindow(parent)
{
    ui.setupUi(this);
    //_login_dialog = new LoginDialog();
    //setCentralWidget(_login_dialog);
    //_login_dialog->show();
}

OurChat::~OurChat()
{}
